# Sandbox

A scratch workspace for manually testing the extension. `F5` (Run Extension) opens the
Extension Development Host with this folder loaded automatically, so **Hired Hand:
Create New Resume** and friends have a workspace to write into without an extra
`File → Open Folder` step.

Everything you create in here (`resume.json`, exports) is gitignored — drop, edit, and
delete freely.
